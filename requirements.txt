# ==================== 核心依赖（一个都不能少）====================
websockets==12.0            # 【根基】连接交易所WebSocket订阅，绝对不能删！
python-dotenv==1.0.0        # 环境变量加载
psutil==5.9.6               # 系统监控

# ==================== 加密解密 ====================
pycryptodome>=3.20.0        # AES-256-GCM 加密解密

# ==================== HTTP客户端（保留，以防万一）====================
aiohttp==3.9.1              # 虽然数据库不用了，但其他HTTP请求可能用到
ccxt==4.2.77                # 交易所API封装


# ==================== MongoDB新增 ====================
pymongo>=4.7.0              # MongoDB驱动（Python 3.10/3.13都支持）
dnspython>=2.6.1            # 解析 mongodb+srv:// 连接串必须

# ==================== 可选开发工具 ====================
# black==23.11.0
# pylint==3.0.3